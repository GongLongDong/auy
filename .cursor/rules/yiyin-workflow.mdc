---
description: 易音项目：文档与 Git 仅在有明确指令时执行
alwaysApply: true
---

# 易音项目 — 写入与提交门禁

## 改 `RULES_YIYIN.md`（及本仓库内规则卡、§6 范例等）

- **默认不要改**：分析、试读、对话里讨论即可。
- **仅当用户明确指令**时才写入，例如：「更新」「更新规则卡」「写入文档」「把刚才的写进 §6」等。**不要**因「看起来该收尾了」或「我帮你更新好吧」而自动改文件。

## Git（`git add` / `git commit` / `git push`）

- **默认不要执行提交**（也不要替用户 push）。
- **仅当用户明确说**要提交、要版本记录、要 git commit（或同义明确指令）时再执行。

## 与 `RULES_YIYIN.md` §0 的关系

- 文档内的「维护约定」与本规则一致；若冲突，以**用户在对话里的最新说法**为准。
